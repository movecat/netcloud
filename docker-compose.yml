services:
  kcloud-ssr:
    build:
      context: .
    image: ${KCLOUD_IMAGE:-kcloud-ssr:legacy}
    container_name: ${KCLOUD_CONTAINER_NAME:-kcloud-ssr}
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./logs:/var/log/kcloud
